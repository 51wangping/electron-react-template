import { useState } from 'react';
import styles from './index.module.less';
const Nav = () => {
  const [count, setstate] = useState(1);
  console.log(2222);

  return <div>消 息</div>;
};
export default Nav;
